node_modules/
.next/
out/
.vercel
.env*
.DS_Store

# local debug / screenshot artifacts (not part of the site)
probe*.html
probe*.txt
*-test.png
*-preview.html
tmp-*
build-*.js

.vercel
.env*
# commit the env template (no secrets) so collaborators can self-configure
!.env.example
